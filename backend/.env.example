# PostgreSQL
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/tu_chamba?schema=public"

# JWT
JWT_SECRET="cambia-esto-por-un-secreto-largo-y-aleatorio"
JWT_EXPIRES_IN="7d"

# Servidor
PORT=3001

# Orígenes permitidos para CORS (web, admin). Separados por coma.
CORS_ORIGINS="http://localhost:3000,http://localhost:3002"

# Google Sign-In (opcional). OAuth Client ID de Google Cloud Console.
# Si no se define, POST /auth/google responde 503 y el botón no se muestra en web.
GOOGLE_CLIENT_ID=""

# URL del sitio web (para los enlaces de verificación de correo).
WEB_URL="http://localhost:3000"

# SMTP para envío de correos (verificación). Si no se define, el enlace de
# verificación se registra en el log del backend (útil en desarrollo).
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="Tu Chamba <no-reply@tuchamba.com>"
