NEXT_PUBLIC_API_URL=http://localhost:3001/api

# Google Sign-In (opcional). Mismo OAuth Client ID que el backend.
# Si no se define, el botón "Continuar con Google" no se muestra.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
